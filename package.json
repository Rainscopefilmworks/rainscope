{
  "name": "rainscope",
  "version": "1.0.0",
  "private": true,
  "description": "Rainscope — production, rentals, shop, and live webcasting",
  "scripts": {
    "fetch-data": "node scripts/fetch-sanity.js",
    "build": "npm run fetch-data && eleventy",
    "dev": "npm run fetch-data && eleventy --serve",
    "studio": "cd studio && npm run dev",
    "seed": "node scripts/seed-sanity.js",
    "worker:deploy": "wrangler deploy --config worker/wrangler.jsonc",
    "worker:tail": "wrangler tail rainscope-square-proxy",
    "site:deploy": "npm run build && wrangler pages deploy _site --project-name=marketing-rainscope",
    "pages:deploy": "npm run site:deploy",
    "pages:deploy:branch": "npm run build && wrangler pages deploy _site --project-name=marketing-rainscope --branch=preview"
  },
  "dependencies": {
    "@sanity/client": "^6.28.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "wrangler": "^4.100.0"
  }
}
