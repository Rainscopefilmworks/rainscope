# Build Tasks: Rainscope Site — Coherence & Analog-Cinematic Pass

Generated from: `.design/rainscope-site/DESIGN_BRIEF.md`, `INFORMATION_ARCHITECTURE.md`, `DESIGN_TOKENS.md`
Date: 2026-07-01

Aesthetic direction for every task below: **analog-cinematic** — deliberate imperfection over polish, grain/texture over sterile gloss, avoid anything that reads as "tech company." Tokens (`--texture-grain`, `--grain-opacity`, `--ease-tactile`, `--duration-shutter`, `--duration-tape-load`, `.grain` utility class) already exist in `css/style.css` — every task here applies them, none of them invent new tokens.

## Foundation

- [x] **Rentals/Shop hero treatment**: Rentals and Shop currently open with a bare `<h1>` + search bar — no hero at all (confirmed: `rentals/index.html` and `shop/index.html` go straight from `{% include "header.njk" %}` into the catalog container). Build a hero moment equivalent in craft to Filmworks/Live's `.hero-reel--cinematic` but suited to a commerce page — doesn't need to be a full-viewport video hero, but needs atmospheric depth (grain, gradient, imagery) rather than a flat container. Use the `.theme-rentals`/`.theme-shop` ocean-blue palette and the `.grain` utility. _Reuses: `.hero-reel--cinematic` structure as a reference pattern, `.grain` utility, existing theme tokens. New: commerce-appropriate hero markup/CSS._ This is the highest-risk/highest-visibility task — do it first so the aesthetic direction for Rentals/Shop is validated before smaller tasks build on it.
  _Done: left-aligned `.commerce-hero` with drifting off-center glow (`.commerce-atmosphere` wrapper on `main.site-content`, spans full-bleed behind the transparent header down through the catalog, no hard seams), `.grain` texture, header now transparent-over-hero via `hero-cinematic` body class + `cinematic-hero.js` reused for the scroll-to-solid transition. Applied to both `rentals/index.html` and `shop/index.html`._

## Core UI

- [x] **Grain application pass — landing gateway**: Apply `.grain`/`.grain--strong` deliberately to the landing gateway's panels and/or background per the brief's "first impression" priority. _Modifies: `index.html` inline styles. Reuses: `.grain` utility._
  _Done: grain overlay on `.panel-bg` (over the imagery, strong opacity) and `.panel-content` (over the glass card, standard opacity). This page uses its own `body.landing-page` class, separate from the site-wide hero-cinematic grain layer below, so it needed its own treatment._
- [x] **Grain application pass — Filmworks & Live**: Apply `.grain` to section-bands and hero overlays on `filmworks/index.html` and `live/index.html`. _Modifies: `filmworks/index.html`, `live/index.html`._
  _Done, then corrected: discovered mid-task that `body.hero-cinematic::after` already provided ONE fixed grain layer over the entire page (predates this design pass). Per-hero and per-section-band grain I'd added were duplicating it and caused visible seams at section boundaries. Removed all the per-element copies (hero's `.hero-grain` div, `.section-band::after`, `.glass-panel::after`) — the pre-existing page-wide layer now covers Filmworks/Live (and Rentals/Shop, and any future hero-cinematic page) with zero seams by construction. See DESIGN_TOKENS.md correction._
- [x] **Grain application pass — Rentals & Shop**: Apply grain to the hero and catalog/section-band regions on `rentals/index.html` and `shop/index.html`. _Depends on: Rentals/Shop hero treatment._
  _Done implicitly: both pages carry `body.hero-cinematic`, so they're already covered by the single site-wide grain layer described above — no per-page work needed once the consolidation landed._
- [x] **Cross-link: Live → Rentals**: Add a reciprocal cross-link line on the Live intro/CTA area pointing to Rentals ("need gear for your own team?"), matching the tone/placement of Filmworks' existing cross-link sentence. _Modifies: `live/index.html` or `_data/sitePages.json` copy. Filmworks→Rentals/Live already exists, no task needed there._
  _Done: appended to the existing `.intro-lead` paragraph, same pattern as Filmworks._
- [x] **Cross-link: Rentals → Filmworks + Live**: Add a reciprocal cross-link line on Rentals pointing to both Filmworks and Live ("need a full crew, not just gear?"). _Modifies: `rentals/index.html` or data file._
  _Done, combined with the secondary CTA below into one `.commerce-hero-crosslink` element._
- [x] **Cross-link: Shop → Rentals**: Add a reciprocal cross-link line on Shop pointing to Rentals ("need it for a shoot, not to keep?"). _Modifies: `shop/index.html` or data file._
  _Done, combined with the secondary CTA below into one `.commerce-hero-crosslink` element._
- [x] **Secondary CTA: "need more than gear?" on Rentals**: Add a lightweight text/banner CTA near the catalog (not a competing form, doesn't touch the existing cart/quote flow in `js/rentals.js`) linking to Filmworks/Live. Can be combined visually/structurally with the Rentals→Filmworks+Live cross-link task above if it makes sense as one element once built — evaluate during implementation. _New: small CTA component. Explicitly does not modify cart logic._
  _Done: combined with the cross-link above — one `.commerce-hero-crosslink` text line in the hero, bordered-top divider under the lead paragraph, no separate banner/form._
- [x] **Secondary CTA: "need more than gear?" on Shop**: Same pattern as above, on `shop/index.html`, linking to Rentals. _New: small CTA component. Explicitly does not modify cart logic._
  _Done: same `.commerce-hero-crosslink` pattern, linking to Rentals._

## Interactions & States

- [x] **Tactile motion on hero load**: Apply `--ease-tactile`/`--duration-tape-load` to the hero entrance moment (Filmworks, Live, and the new Rentals/Shop hero) so it feels like a deliberate "tape-load" rather than a generic fade. Must respect `prefers-reduced-motion` (already globally handled in `css/style.css`, verify it still applies cleanly to whatever new transition is added). Covers: initial hero load state only — not scroll-driven parallax, which already exists and works.
  _Done: `hero-content-load` keyframe (opacity + slight rise + blur-focus pull that clears before the motion finishes) on `.hero-reel-content` (Filmworks/Live) and `.commerce-hero-inner` (Rentals/Shop), shared `@keyframes` definition. `prefers-reduced-motion` already forces all `animation-duration` to 0.01ms globally, so this collapses to the end state automatically — no extra handling needed._
- [x] **Tactile motion on modal/gallery open**: Apply `--ease-tactile`/`--duration-shutter` to `.work-modal`, `.commerce-cart-panel` open/close transitions, and any lightbox/gallery interactions, so these feel like a deliberate shutter-click rather than a standard fade/slide. Covers: open, close, and backdrop states. Do not touch the underlying cart/modal logic in `js/rentals.js`/`js/shop.js`/`js/filmworks-work.js` — visual transition timing/easing only.
  _Done: `.work-modal` had zero open/close transition before (`display:none/flex` hard toggle) — added opacity + content scale via `@starting-style`/`allow-discrete`. Mobile `.commerce-cart-panel` bottom sheet got the same treatment plus a slide-up (`translateY`). The generic native `dialog`/`.modal-content` system (FAQ modal, etc.) already had working transition infrastructure — just retuned its tokens from `--transition-normal` to `--duration-shutter`/`--ease-tactile` rather than rebuilding it. No JS touched._
- [x] **Button interaction polish**: Evaluate whether `.btn-primary`/`.btn-secondary`'s current `translateY(-2px)` hover lift should shift toward `--ease-tactile`/`--duration-shutter` for a more mechanical feel, or whether it already reads fine once surrounding grain/texture changes land. Covers: hover, active/pressed states (existing `:active` scale-down stays).
  _Done: swapped the base `.btn` transition from `var(--transition-normal)` (0.3s smooth ease) to `--duration-shutter`/`--ease-tactile` — governs hover lift and the existing `:active` press-down uniformly, snappier/more mechanical without changing the lift/scale values themselves._
- [x] **Card treatment consistency pass**: Review `.service-card`, `.film-card`, `.gear-card`, `.team-card` against the analog direction — this is a polish pass (texture, hover feel), not a redesign of structure or content layout. Covers: default, hover, and (for `.gear-card`) loading-skeleton states — verify `.catalog-skeleton-*` still reads correctly once grain is introduced nearby.
  _Reviewed: all four already carry deliberate, non-generic hover treatments from the prior "visual depth" pass (custom cubic-beziers, lift+scale, avatar/poster zoom) — not a "tech company" pattern, no redesign needed. Found and fixed one real inconsistency: `.film-card` had spring-green hardcoded as raw `rgba(49, 232, 128, ...)` instead of `var(--color-accent-rgb)` (theme-aware everywhere else). `.gear-stock`'s hardcoded green is correct as-is — that's a semantic in-stock/low-stock/out-of-stock status color, not a theme accent, so it should stay green regardless of division. Grain no longer sits over cards at all (it's the single page-wide layer at z-index:0, behind content at z-index:1), so the skeleton-loader concern in this task is moot — nothing to verify there._

## Responsive & Polish

- [x] **Testimonials: extend site-wide or confirm Filmworks-only**: Confirmed via codebase check — `testimonials.njk` is currently only included on `filmworks/index.html`. Decide whether to extend it to Live/Rentals/Shop (using each division's own testimonial content, if available) for the "connected" feeling the brief calls for, or explicitly confirm it stays Filmworks-only and note why. _Depends on: content availability — check with Sanity data before committing to site-wide extension._
  _Done: `_data/testimonials.json` already had a `tags` field per testimonial (unused until now) — Filmworks has 3, Rentals has 1 (shared with Filmworks), Live/Shop have 0. Added a `withTag` eleventy filter and made `testimonials.njk` filter by an optional `testimonialTag`, auto-hiding the indicator dots when there's only one result. Extended to Rentals (content-supported); Live/Shop stay without a testimonials section since there's no tagged content for them — not a placeholder/fake-content situation. Also moved the testimonials CSS out of Filmworks' page-local `<style>` block into the shared `css/style.css`, since it's now a genuinely shared component (the JS in `main.js` already was)._
- [x] **Landing gateway mobile re-verification**: A prior commit (`156bdbc`) fixed a mobile layout overlap bug on this exact page. After the grain/coherence changes land here, re-verify at 375px width specifically — this page has a history of mobile regressions and any visual changes need explicit re-checking, not just an assumption that the old fix still holds.
  _Verified by designer at ~375px — no regressions from the grain overlay._
- [x] **Responsive check — new Rentals/Shop hero**: Verify the new hero treatment (Foundation task) across 375px / 768px / 1280px+ — this is new markup with no existing responsive precedent, unlike the rest of the page which reuses the proven `.catalog-grid` breakpoint system.
  _Structural review: hero uses fluid `clamp()` sizing throughout rather than fixed breakpoint jumps; only hard breakpoint is removing the title's `16ch` cap below 767px to avoid cramped wrapping. Verified by designer at ~375px and ~768px — no issues._
- [x] **Accessibility pass — contrast + focus rings**: Verify WCAG AA (4.5:1 text, 3:1 UI) holds for all three theme accents against their backgrounds — spring-green-on-evergreen (Filmworks), ocean-blue-on-onyx (Rentals/Shop), lavender-on-black (Live) — in both body text and `:focus-visible` ring use, per the brief's accessibility requirements. Also spot-check that grain overlays (`opacity: 0.05–0.09`, `mix-blend-mode: overlay`) never sit between real text and its background in a way that could pull contrast down on the lightest-text/darkest-background combinations.
  _Computed WCAG contrast ratios for all 3 theme accents + secondary text against their backgrounds — all pass comfortably (range 5.48:1–11.28:1, tightest is Rentals/Shop accent at 5.48:1, still above the 4.5:1 text / 3:1 UI floor). `:focus-visible` reuses the accent color so it inherits the same clearance. Grain-vs-text concern is structurally moot post-consolidation: grain is a single page-wide layer at z-index:0, behind all content (z-index:1), so it can never sit between text and its background._
- [x] **Filmworks `.film-grid` mobile columns** _(not in original list — designer feedback during review)_: Below 480px, `.film-grid` was force-overridden to a single column, even though the base rule already defines 2 columns. Removed the dead `@media (max-width: 480px)` override so film posters stay at 2 columns minimum on mobile.

## Review

- [x] **Design review**: Run `/design-review` against `DESIGN_BRIEF.md` once the above is built — check specifically for any component that still reads as "tech company" per the brief's core anti-reference, and verify all four divisions feel distinct-yet-connected per the palette/grain system.
  _Done: full review in `DESIGN_REVIEW.md`. One Must Fix (Live's "Recent Productions" placeholder renders as an unlabeled blank box — CMS has only a single "COMING SOON" item and the caption is hover-only with no base visible state). Two Could Improve notes (landing gateway's 3-panel symmetry is the closest thing on site to the brief's SaaS anti-reference; service icons use plain emoji). No Should Fix issues. Grain system, hero identity, work modal, cross-links, cart UX, focus rings, and responsive behavior all confirmed working well._
